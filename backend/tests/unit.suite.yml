# backend/tests/unit.suite.yml
suite_namespace: backend\tests\unit
actor: UnitTester
modules:
  enabled:
    - Asserts
    - Yii2:
        part: [orm, fixtures] # <--- ESTA LINHA É OBRIGATÓRIA PARA O seeRecord
        entryScript: index-test.php
